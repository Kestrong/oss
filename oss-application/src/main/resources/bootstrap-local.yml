spring:
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false

oss:
  enable: true
  defaultApiType: minio
  minio:
    defaultBucket: test
    url: http://10.11.2.187:9000
    accessKey: minio
    secretKey: Itsm@iod#520
    enable: true